\floatname{algorithm}{算法}  
\renewcommand{\algorithmicrequire}{\textbf{输入:}}  
\renewcommand{\algorithmicensure}{\textbf{输出:}} 

\begin{algorithm}
	\caption{\textbf{自适应对抗训练方法}} 
	\label{alg3-1}
	\begin{algorithmic}
		% 输入
		\REQUIRE 原始样本$X=[q;c]$, 由X生成的对抗样本$X_{adv}=[q';a']$，标签$y$，目标模型$F(\cdot)$，损失函数$Loss(\cdot)$，损失提升率$\epsilon$，损失权重$\alpha$
		% 输出
		\ENSURE 无
		% 初始化
        \STATE $loss_{clean} = Loss(F(X), y)$\; %\;用于换行
        \STATE $loss_{adv} = Loss(F(X_{adv}), y)$\;
        \IF{$loss_{adv} < \epsilon * loss_{clean}$}
            \STATE $\quad X_{adv} \leftarrow $ Sentence\_Gradient\_Attack($X$)\;
            \STATE $\quad loss_{adv} = Loss(F(X_{adv}), y)$\;
        \ENDIF
        \STATE $loss = \alpha*loss_{clean} + (1-\alpha) * loss_{adv}$\;
        \STATE $loss.backward()$\;
	\end{algorithmic}
\end{algorithm}

% \begin{algorithm}
%     \label{alg3-1}
%     \caption{Self-Adaptive Adversarial Training Method}%算法名字
%     \LinesNumbered %要求显示行号
%     \KwIn{original example $X=[q;a]$, adversarial example $X_{adv}=[q';a']$ generated by $X$, label $y$, target model $F(\cdot)$, loss function $Loss(\cdot)$, loss increase ratio $\epsilon$, loss weight $\alpha$
%     }%输入参数
%     \KwOut{None}%输出
%     $loss_{clean} = Loss(F(X), y)$\; %\;用于换行
%     $loss_{adv} = Loss(F(X_{adv}), y)$\;
%     \If{$loss_{adv} < \epsilon * loss_{clean}$}{
%             $X_{adv} \leftarrow $ Sentence\_Gradient\_Attack($X$)\;
%             $loss_{adv} = Loss(F(X_{adv}), y)$\;
%     }
%     $loss = \alpha*loss_{clean} + (1-\alpha) * loss_{adv}$\;
%     $loss.backward()$\;
% \end{algorithm}